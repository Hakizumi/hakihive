server:
  port: 11622

spring:
  application:
    name: Cepheuna

  ai:
    openai:
      api-key: sk-xxxx   # You configure
      base-url: https://api.openai.com

cepheuna:
  models:
    # Models config properties,see org/hakizumi/cepheuna/config/ModelProperties
    decision-model: gpt-4  # main reply model

  tools:
    # config about ai-agent-tools
    io-tools:
      # see org.sempiria.cepheuna.tools.IOTools
      whitelist:
        - E:/safe_path    # Safe path
      blacklist:
        # blacklist paths ( model cannot access )
        - /    # All

      # If the whitelist and blacklist have duplicated entries, an IllegalArgumentException will be called

logging:
  level:
    root: info
    org.hakizumi.cepheuna: debug